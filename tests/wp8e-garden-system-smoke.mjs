import assert from 'node:assert/strict';
import { getHeroDefinition } from '../src/content/heroes/HeroDefinitions.js';import { ensureHeroRuntime } from '../src/sim/heroes/HeroSystem.js';import { HeroGardenSystem } from '../src/sim/heroes/HeroGardenSystem.js';
const d=getHeroDefinition('hero.sleeping-gardener');const h={id:'gardener',heroId:d.id,role:d.role,name:d.displayName,faction:'dungeon',ecologyFaction:d.factionId,roomId:'F29',roomCell:{x:0,z:0},alive:true,hp:d.baseStats.hp,maxHp:d.baseStats.hp,attack:d.baseStats.attack,armor:d.baseStats.armor,courage:d.baseStats.courage,size:d.size};ensureHeroRuntime(h,d);const ally={id:'a',faction:'dungeon',ecologyFaction:d.factionId,roomId:'F29',alive:true,hp:5,maxHp:20};const enemy={id:'e',faction:'party',roomId:'F29',alive:true,hp:50,maxHp:50,attack:7};const blight={id:'blight',kind:'death-blight',roomId:'F29',remaining:10,ownerId:'x'};const sys=new HeroGardenSystem();const sim={agents:[h,ally,enemy],heroEnvironmentSystem:{fields:[blight]},heroSkillSystem:{zones:[]},heroResourceLedger:{},applyCombatDamage:(_s,t,a)=>t.hp-=a,emitEffect(){}};
assert.equal(sys.rootedOrchard(h,{duration:5,radius:5,healPerSecond:2,hostileRoot:1.2},sim),true);sys.update(1.1,sim);assert.ok(ally.hp>5);assert.ok(enemy.heroStatuses.gardenRoot);assert.equal(sys.isMovementBlocked(enemy),true);
assert.equal(sys.pruneBlight(h,{damage:10,removeFields:2,biomassGain:3},sim),true);assert.equal(blight.remaining,0);assert.ok(enemy.hp<50);assert.equal(sim.heroResourceLedger[d.factionId].biomass,3);
const before=h.heroSeason;assert.equal(sys.turnSeasons(h,{order:['spring','summer','autumn','winter'],duration:4},sim),true);assert.notEqual(h.heroSeason,before);assert.ok(h.heroStatModifiers.season);assert.doesNotThrow(()=>JSON.stringify(sys.snapshot()));
console.log('WP8-E garden system smoke passed');
