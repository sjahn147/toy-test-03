import assert from 'node:assert/strict';
import { getHeroDefinition } from '../src/content/heroes/HeroDefinitions.js';import { ensureHeroRuntime } from '../src/sim/heroes/HeroSystem.js';import { HeroHoardSystem } from '../src/sim/heroes/HeroHoardSystem.js';import { HeroPhysicsSystem } from '../src/sim/heroes/HeroPhysicsSystem.js';
const d=getHeroDefinition('hero.goldcrown-back');const h={id:'gold',heroId:d.id,role:d.role,name:d.displayName,faction:'dungeon',ecologyFaction:d.factionId,roomId:'L58',roomCell:{x:0,z:0},alive:true,hp:d.baseStats.hp,maxHp:d.baseStats.hp,attack:d.baseStats.attack,armor:d.baseStats.armor,courage:d.baseStats.courage,size:d.size};ensureHeroRuntime(h,d);const e1={id:'e1',role:'fighter',faction:'party',roomId:'L58',roomCell:{x:0,z:2},alive:true,hp:60,maxHp:60,attack:7,armor:3,size:'medium'};const e2={id:'e2',role:'cleric',faction:'party',roomId:'L58',roomCell:{x:.4,z:4},alive:true,hp:50,maxHp:50,attack:6,armor:2,size:'medium'};const prop={id:'relic',roomId:'L58',type:'royal-relic',opened:false};const sys=new HeroHoardSystem();const physics=new HeroPhysicsSystem();const sim={agents:[h,e1,e2],rooms:[{id:'L58',w:12,d:12}],props:[prop],corpses:[],heroPhysicsSystem:physics,applyCombatDamage:(_s,t,a)=>t.hp-=a};
assert.equal(sys.passiveScavenge(h,sim),true);assert.equal(prop.consumed,true);assert.ok(h.hoardArmor>0);const raw=20,mitigated=sys.modifyIncomingDamage(e1,h,raw);assert.ok(mitigated<raw);
assert.equal(sys.boneRakeCharge(h,'e1',{damage:12,length:6,width:1.7,impulse:6,armorSteal:1},sim),true);assert.ok(e1.hp<60);assert.ok(physics.impulses.length>0);
h.hoardArmor=4;assert.equal(sys.trophyVolley(h,{maximum:3,damage:8,armorCostPerShot:1},sim),true);assert.equal(sys.projectiles.length,2);sys.update(1.2,sim);assert.ok(e1.hp<48||e2.hp<50);
assert.equal(sys.royalMolt(h,{duration:5,shellHp:70,speedMultiplier:1.3,attackBonus:5,lootRelease:2},sim),true);assert.equal(sys.shells.length,1);assert.equal(h.heroVariant,'ravenous-molt');assert.doesNotThrow(()=>JSON.stringify(sys.snapshot()));
console.log('WP8-E hoard system smoke passed');
