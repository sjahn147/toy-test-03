name: WP10 Production Camera

on:
  pull_request:
  push:
    branches: [main]

jobs:
  camera-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run test:camera-v10
