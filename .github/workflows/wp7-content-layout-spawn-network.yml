name: WP7 Content Layout and Spawn Network

on:
  pull_request:
  push:
    branches: [main]

jobs:
  wp7:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run test:wp7
